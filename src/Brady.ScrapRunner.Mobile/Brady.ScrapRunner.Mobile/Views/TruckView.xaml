<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Brady.ScrapRunner.Mobile.ViewModels;assembly=Brady.ScrapRunner.Mobile"
             xmlns:i18N="clr-namespace:Brady.ScrapRunner.Mobile.Resources;assembly=Brady.ScrapRunner.Mobile"
             x:Class="Brady.ScrapRunner.Mobile.Views.TruckView"
             Title="{Binding Title}">
  <ContentPage.BindingContext>
    <vm:TruckViewModel/>
  </ContentPage.BindingContext>
  <StackLayout Orientation="Vertical" VerticalOptions="CenterAndExpand" x:Name="RedUnderlineHack">
    <Label Text="{i18N:Translate TruckId}" />
    <Entry Text="{Binding TruckId}"/>
    <Label Text="{i18N:Translate Odometer}"/>
    <Entry Text="{Binding Odometer}"/>
    <Button Text="{i18N:Translate Continue}" HorizontalOptions="Fill" Command="{Binding ContinueCommand}"/>
  </StackLayout>
</ContentPage>